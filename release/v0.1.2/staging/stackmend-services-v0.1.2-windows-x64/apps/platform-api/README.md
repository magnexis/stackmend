# STACKMEND Platform API

This app provides service-level access to repository intelligence, reports, policies, and repair planning for CI, teams, and enterprise workflows.

## Current scaffold

- buildable workspace package
- `status` command for machine-readable service metadata
- `serve` command with a minimal health-style HTTP response
